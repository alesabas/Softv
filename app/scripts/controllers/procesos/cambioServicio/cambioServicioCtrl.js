'use strict';

angular
    .module('softvApp')
    .controller('cambioServicioCtrl', function ( CatalogosFactory,ngNotify, atencionFactory, $uibModal, $localStorage) {
        
        function initData(){  

        }

        var vm = this;
        initData();
        
    });