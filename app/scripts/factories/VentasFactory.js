'use strict';

angular
    .module('softvApp')
    .factory('VentasFactory', function($http, $q, globalService, $localStorage){

        var factory = {};

        var paths = {};

        return factory;

    });